# 多模态 RAG 聊天机器人 - 测试逻辑设计

## 1. 测试策略概述

采用分层测试策略：
- **单元测试**：验证单个函数/模块的正确性
- **集成测试**：验证模块间协作
- **E2E 测试**：验证完整业务流程

## 2. 单元测试

### 2.1 文档解析测试

```python
# tests/unit/test_document_parser.py

def test_split_text2chunks():
    """测试文本分块功能"""
    from workers.document_processor import split_text2chunks
    
    # 测试数据
    lines = [
        "# 标题1",
        "这是第一段内容。",
        "",
        "# 标题2",
        "这是第二段内容，包含更多文字。",
        "[1] 参考文献",
        "# References"
    ]
    
    # 执行
    chunks = split_text2chunks(lines, chunk_size=50)
    
    # 验证
    assert len(chunks) > 0
    assert all(len(chunk) <= 50 for chunk in chunks)
    assert "参考文献" not in str(chunks)  # 应该过滤参考文献
    assert "References" not in str(chunks)


def test_encode_text():
    """测试文本编码功能"""
    from services.embedding_service import encode_text
    
    text = "这是一个测试文本"
    embedding = encode_text(text)
    
    # 验证
    assert embedding is not None
    assert len(embedding) == 512  # BGE-small 输出维度
    assert isinstance(embedding, list)
    assert all(isinstance(x, float) for x in embedding)
```

### 2.2 向量检索测试

```python
# tests/unit/test_retrieval.py

def test_milvus_search():
    """测试 Milvus 向量检索"""
    from utils.milvus_client import search_vectors
    
    # 构造测试查询向量
    query_vector = [0.1] * 512
    
    # 执行搜索
    results = search_vectors(
        collection_name="rag_data_new",
        query_vector=query_vector,
        top_k=5
    )
    
    # 验证
    assert len(results) <= 5
    assert all("text" in result for result in results)
    assert all("score" in result for result in results)


def test_search_with_filter():
    """测试带过滤条件的检索"""
    results = search_vectors(
        collection_name="rag_data_new",
        query_vector=[0.1] * 512,
        top_k=5,
        filter="file_name == 'test.pdf'"
    )
    
    assert all(r["file_name"] == "test.pdf" for r in results)
```

### 2.3 文件上传测试

```python
# tests/unit/test_upload.py

def test_file_save():
    """测试文件保存逻辑"""
    from services.upload_service import save_uploaded_file
    import tempfile
    
    # 创建临时文件模拟上传
    with tempfile.NamedTemporaryFile(suffix=".pdf", delete=False) as tmp:
        tmp.write(b"fake pdf content")
        tmp_path = tmp.name
    
    # 执行保存
    file_id, save_path = save_uploaded_file(tmp_path, "test.pdf")
    
    # 验证
    assert file_id is not None
    assert os.path.exists(save_path)
    assert save_path.endswith(".pdf")
```

## 3. 集成测试

### 3.1 上传-解析流程测试

```python
# tests/integration/test_upload_parse_flow.py

@pytest.mark.integration
def test_upload_and_parse_flow():
    """测试完整的上传-解析流程"""
    import time
    
    # 1. 上传测试文件
    response = client.post(
        "/upload/document",
        files={"file": ("test.pdf", open("tests/data/sample.pdf", "rb"), "application/pdf")}
    )
    assert response.status_code == 200
    file_id = response.json()["data"]["file_id"]
    
    # 2. 验证数据库记录
    from models.file_model import File, Session
    with Session() as session:
        file_record = session.query(File).filter_by(id=file_id).first()
        assert file_record is not None
        assert file_record.filestate == "已上传"
    
    # 3. 验证 Kafka 消息
    # 消费 rag-data topic，验证消息内容
    
    # 4. 等待 Worker 处理
    time.sleep(60)  # 等待解析完成
    
    # 5. 验证解析完成
    with Session() as session:
        file_record = session.query(File).filter_by(id=file_id).first()
        assert file_record.filestate == "已完成"
    
    # 6. 验证 Milvus 数据
    from utils.milvus_client import client
    results = client.query(
        collection_name="rag_data_new",
        filter=f"db_id == {file_id}"
    )
    assert len(results) > 0  # 应该插入至少一个 chunk
```

### 3.2 问答流程测试

```python
# tests/integration/test_chat_flow.py

@pytest.mark.integration
def test_chat_with_existing_data():
    """测试问答流程（假设已有索引数据）"""
    
    # 1. 发送问题
    response = client.post(
        "/chat",
        json={"question": "测试文档的主要内容是什么？"}
    )
    
    # 2. 验证响应
    assert response.status_code == 200
    data = response.json()["data"]
    assert "answer" in data
    assert "sources" in data
    assert len(data["sources"]) > 0
    assert len(data["sources"]) <= 5  # top-5
    
    # 3. 验证答案相关性（简单检查）
    answer = data["answer"]
    assert len(answer) > 0
    assert "无法回答" not in answer or "根据资料" in answer
```

## 4. E2E 测试

### 4.1 完整用户场景测试

```python
# tests/e2e/test_full_scenario.py

@pytest.mark.e2e
def test_full_user_scenario():
    """
    完整用户场景：
    1. 用户上传 PDF
    2. 等待处理完成
    3. 用户提问
    4. 验证回答
    """
    # Step 1: 上传文档
    with open("tests/data/report_with_chart.pdf", "rb") as f:
        response = client.post(
            "/upload/document",
            files={"file": ("report.pdf", f, "application/pdf")}
        )
    file_id = response.json()["data"]["file_id"]
    
    # Step 2: 轮询等待处理完成
    max_wait = 120  # 最多等待2分钟
    for _ in range(max_wait):
        response = client.get(f"/files/{file_id}/status")
        if response.json()["data"]["status"] == "已完成":
            break
        time.sleep(1)
    else:
        pytest.fail("文档处理超时")
    
    # Step 3: 用户提问
    questions = [
        "这份报告的主题是什么？",
        "图表显示了什么趋势？",
        "第三页的设备是什么？"
    ]
    
    for question in questions:
        response = client.post("/chat", json={"question": question})
        assert response.status_code == 200
        answer = response.json()["data"]["answer"]
        
        # 验证答案质量
        assert len(answer) > 10  # 答案不能太短
        assert len(answer) < 2000  # 答案不能太长


@pytest.mark.e2e
def test_multimodal_image_question():
    """测试需要理解图片的问题"""
    # 上传包含图表的文档
    # 提问关于图表内容
    # 验证回答包含图表相关信息
    pass
```

## 5. 性能测试

```python
# tests/performance/test_performance.py

@pytest.mark.performance
def test_concurrent_uploads():
    """测试并发上传性能"""
    import concurrent.futures
    
    def upload_file(i):
        with open("tests/data/small.pdf", "rb") as f:
            return client.post(
                "/upload/document",
                files={"file": (f"file_{i}.pdf", f, "application/pdf")}
            )
    
    start = time.time()
    with concurrent.futures.ThreadPoolExecutor(max_workers=10) as executor:
        futures = [executor.submit(upload_file, i) for i in range(10)]
        results = [f.result() for f in futures]
    
    elapsed = time.time() - start
    assert elapsed < 10  # 10个并发上传应在10秒内完成
    assert all(r.status_code == 200 for r in results)


@pytest.mark.performance
def test_chat_response_time():
    """测试问答响应时间"""
    start = time.time()
    response = client.post("/chat", json={"question": "测试问题"})
    elapsed = time.time() - start
    
    assert response.status_code == 200
    assert elapsed < 5  # 响应时间应小于5秒
```

## 6. 测试数据准备

### 6.1 测试文件

```
tests/data/
├── sample.pdf              # 简单文本 PDF
├── report_with_chart.pdf   # 包含图表的 PDF
├── multi_image.pdf         # 包含多张图片的 PDF
├── empty.pdf               # 空 PDF（边界测试）
├── large.pdf               # 大文件（性能测试）
└── corrupted.pdf           # 损坏文件（异常测试）
```

### 6.2 测试夹具

```python
# tests/conftest.py

import pytest

@pytest.fixture
def mock_milvus_client():
    """Mock Milvus 客户端"""
    class MockClient:
        def search(self, **kwargs):
            return [
                {"text": "测试文本1", "score": 0.95, "file_name": "test.pdf"},
                {"text": "测试文本2", "score": 0.85, "file_name": "test.pdf"},
            ]
    return MockClient()

@pytest.fixture
def sample_pdf_path():
    """返回测试 PDF 路径"""
    return "tests/data/sample.pdf"

@pytest.fixture
def db_session():
    """提供数据库会话"""
    from models.file_model import Session
    session = Session()
    yield session
    session.rollback()
    session.close()
```

## 7. 测试执行计划

| 测试类型 | 执行时机 | 命令 |
|---------|---------|------|
| 单元测试 | 每次提交 | `pytest tests/unit -v` |
| 集成测试 | PR 合并前 | `pytest tests/integration -v --integration` |
| E2E 测试 | 发布前 | `pytest tests/e2e -v --e2e` |
| 性能测试 | 定期 | `pytest tests/performance -v --performance` |

## 8. 覆盖率目标

| 模块 | 目标覆盖率 |
|------|-----------|
| services/ | >= 80% |
| workers/ | >= 70% |
| utils/ | >= 75% |
| models/ | >= 90% |
| 整体 | >= 75% |
