# LangChain 基础

LangChain 是一个面向大模型应用开发的框架，常用于封装模型调用、Prompt 模板、工具调用、文档检索和链式编排。

对于 Java 后端开发者来说，可以把 LangChain 理解成一套“模型应用 SDK”。它帮助开发者把大模型调用、外部工具、业务上下文和用户输入组织成稳定的执行流程。

在本地知识库问答中，LangChain 通常负责把用户问题、检索到的文档片段和系统提示词组合起来，再调用大模型生成回答。
