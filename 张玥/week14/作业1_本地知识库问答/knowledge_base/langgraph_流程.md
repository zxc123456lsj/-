# LangGraph 流程编排

LangGraph 更适合处理带状态、可分支、可循环的 Agent 工作流。

普通 LangChain Chain 更像一条固定流水线，而 LangGraph 更像一个带状态机能力的流程引擎。每个节点可以完成一个独立步骤，例如意图识别、工具调用、结果校验、人工确认和最终回答。

如果任务需要多轮决策、条件路由、失败重试或人工干预，LangGraph 通常比简单链式调用更清晰。
