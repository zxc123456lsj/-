# 代码逐行详细解释

## 1. 导入库和读取数据部分

```python
"""
1、调整 09_深度学习文本分类.py 代码中模型的层数和节点个数，对比模型的loss变化。
"""
```

- **第1行**: 注释说明本文件的任务是调整模型结构并比较损失函数的变化

```python
import pandas as pd
import torch
import torch.nn as nn
import torch.optim as optim
from torch.utils.data import Dataset, DataLoader
```

- **第2-6行**: 导入必要的库，包括pandas用于数据处理，torch及其子模块用于深度学习

```python
dataset = pd.read_csv("dataset.csv", sep="\t", header=None)
texts = dataset[0].tolist()
string_labels= dataset[1].tolist()
```

- **第8行**: 从CSV文件中读取数据集，使用制表符作为分隔符
- **第9行**: 提取文本列并转换为列表
- **第10行**: 提取标签列并转换为列表

```python
label_to_index = {label: i for i, label in enumerate(set(string_labels))}
numerical_labels = [label_to_index[label] for label in string_labels]
```

- **第12行**: 创建标签到数字索引的映射字典
- **第13行**: 将原始字符串标签转换为数值标签

## 2. 字符到索引映射构建

```python
char_to_index = {'<pad>': 0}
for text in texts:
    for char in text:
        if char not in char_to_index:
            char_to_index[char] = len(char_to_index)

index_to_char = {i: char for char, i in char_to_index.items()}
vocab_size = len(char_to_index)
```

- **第15行**: 初始化字符到索引的映射，保留0给填充字符
- **第16-17行**: 遍历所有文本中的字符，建立字符到索引的映射
- **第19行**: 创建反向映射（索引到字符）
- **第20行**: 获取词汇表大小

```python
max_len = 40
```

- **第22行**: 设置最大文本长度为40个字符

## 3. 自定义数据集类

```python
class CharBoWDataset(Dataset):
    def __init__(self, texts, labels, char_to_index, max_len, vocab_size):
        self.texts = texts
        self.labels = torch.tensor(labels, dtype=torch.long)
        self.char_to_index = char_to_index
        self.max_len = max_len
        self.vocab_size = vocab_size
        self.bow_vectors = self._create_bow_vectors()
```

- **第24行**: 定义字符级词袋数据集类，继承自Dataset
- **第25行**: 类初始化方法
- **第26-32行**: 初始化各种参数，创建词袋向量

```python
    def _create_bow_vectors(self):
        tokenized_texts = []
        for text in self.texts:
            tokenized = [self.char_to_index.get(char, 0) for char in text[:self.max_len]]
            tokenized += [0] * (self.max_len - len(tokenized))
            tokenized_texts.append(tokenized)

        bow_vectors = []
        for text_indices in tokenized_texts:
            bow_vector = torch.zeros(self.vocab_size)
            for index in text_indices:
                if index !=0:
                    bow_vector[index] += 1
            bow_vectors.append(bow_vector)
        return torch.stack(bow_vectors)
```

- **第34行**: 私有方法，创建词袋向量
- **第35-39行**: 将每个文本转换为字符索引序列，不足长度的部分用0填充
- **第40-46行**: 为每个文本创建词袋向量（统计字符出现频次）
- **第47行**: 返回堆叠后的张量

```python
    def __len__(self):
        return len(self.texts)

    def __getitem__(self, idx):
        return self.bow_vectors[idx], self.labels[idx]
```

- **第49-50行**: 实现获取数据集长度的方法
- **第52-53行**: 实现获取指定索引数据的方法

## 4. 神经网络模型定义

```python
class SimpleClassifier(nn.Module):
    def __init__(self, input_dim, hidden_dim, output_dim):
        super(SimpleClassifier, self).__init__()
        self.fc1 = nn.Linear(input_dim, hidden_dim)
        self.relu = nn.ReLU()
        self.fc2 = nn.Linear(hidden_dim, output_dim)

    def forward(self, x):
        out = self.fc1(x)
        out = self.relu(out)
        out = self.fc2(out)
        return out
```

- **第56行**: 定义简单分类器类，继承自nn.Module
- **第57行**: 初始化方法
- **第58-60行**: 定义两层线性变换和激活函数
- **第62行**: 前向传播方法
- **第63-65行**: 按顺序执行线性变换、激活函数、线性变换

## 5. 数据加载和模型配置

```python
char_dataset = CharBoWDataset(texts, numerical_labels, char_to_index, max_len, vocab_size)
dataloader = DataLoader(char_dataset, batch_size=32, shuffle=True)

hidden_dim = 128
output_dim = len(label_to_index)
model = SimpleClassifier(vocab_size, hidden_dim, output_dim)
criterion = nn.CrossEntropyLoss()
optimizer = optim.SGD(model.parameters(), lr=0.01)
```

- **第68行**: 创建字符词袋数据集实例
- **第69行**: 创建数据加载器，批次大小为32，随机打乱数据
- **第71行**: 设置隐藏层维度为128
- **第72行**: 输出维度等于标签种类数量
- **第73行**: 创建模型实例
- **第74行**: 定义交叉熵损失函数
- **第75行**: 定义SGD优化器，学习率为0.01

## 6. 训练循环

```python
num_epochs = 10
for epoch in range(num_epochs):
    model.train()
    running_loss = 0.0
    for idx, (inputs, labels) in enumerate(dataloader):
        optimizer.zero_grad()
        outputs = model(inputs)
        loss = criterion(outputs, labels)
        loss.backward()
        optimizer.step()
        running_loss += loss.item()
        if idx % 50 == 0:
            print(f"Batch 个数 {idx}, 当前Batch Loss：{loss.item()}")

    print(f"Epoch [{epoch + 1}/{num_epochs}], Loss：{running_loss / len(dataloader):.4f}")
```

- **第77行**: 设置训练轮数为10
- **第78行**: 外层循环遍历每个epoch
- **第79行**: 设置模型为训练模式
- **第80行**: 初始化当前epoch的累积损失
- **第81行**: 内层循环遍历数据加载器
- **第82行**: 清零梯度
- **第83行**: 前向传播获得输出
- **第84行**: 计算损失值
- **第85行**: 反向传播计算梯度
- **第86行**: 更新模型参数
- **第87行**: 累加损失值
- **第88-89行**: 每50个批次打印一次批次损失
- **第91行**: 打印当前epoch的平均损失

## 7. 文本分类函数

```python
def classify_text(text, model, char_to_index, vocab_size, max_len, index_to_label):
    tokenized = [char_to_index.get(char, 0) for char in text[:max_len]]
    tokenized += [0] * (max_len - len(tokenized))

    bow_vector = torch.zeros(vocab_size)
    for index in tokenized:
        if index != 0:
            bow_vector[index] += 1

    bow_vector = bow_vector.unsqueeze(0)

    model.eval()
    with torch.no_grad():
        output = model(bow_vector)

    _, predicted_index = torch.max(output, 1)
    predicted_index = predicted_index.item()
    predicted_label = index_to_label[predicted_index]

    return predicted_label
```

- **第94行**: 定义文本分类函数
- **第95-96行**: 将输入文本转换为字符索引并填充
- **第98-101行**: 创建词袋向量，统计字符频次
- **第103行**: 在第0维添加批次维度
- **第105行**: 设置模型为评估模式
- **第106-107行**: 关闭梯度计算进行推理
- **第109-110行**: 获取预测概率最高的类别索引
- **第111-113行**: 转换为标签名称并返回

## 8. 测试预测

```python
index_to_label = {i: label for label, i in label_to_index.items()}

new_text = "帮我导航到北京"
predicted_class = classify_text(new_text, model, char_to_index, vocab_size, max_len, index_to_label)
print(f"输入 ‘{new_text}’ 预测为：‘{predicted_class}’")

new_text_2 = "查询明天北京的天气"
predicted_class2 = classify_text(new_text_2, model, char_to_index, vocab_size, max_len, index_to_label)
print(f"输入 ‘{new_text_2}’ 预测为：‘{predicted_class2}")
```

- **第116行**: 创建索引到标签的反向映射
- **第118-119行**: 对新文本进行分类预测并打印结果
- **第121-122行**: 对另一个新文本进行分类预测并打印结果